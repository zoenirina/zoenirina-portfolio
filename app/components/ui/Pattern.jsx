'use client';

import React from 'react'
import './pattern.css'

const Pattern = () => {
  return (
    <div className="pattern-container w-full h-[800px] bg-blue-50 relative overflow-hidden">
    </div>
  );
};

export default Pattern;
